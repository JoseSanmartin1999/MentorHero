# MentorHero
Proyecto de Titulación para Obtener el Grado de Ing. en Software 

npm install express mongoose bcryptjs dotenv cors
Paquete,Propósito
express,El framework principal para crear el servidor.
mongoose,ODM (Object Data Modeling) para interactuar con MongoDB.
bcryptjs,Se usará para cifrar (hashear) las contraseñas de manera segura.
dotenv,Para cargar variables de entorno (como la URL de la DB) desde un archivo .env.
cors,Middleware para permitir peticiones del frontend (que corre en un puerto diferente).